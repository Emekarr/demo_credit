import ConfigType from './type.config';

const development: ConfigType = {
	database: 'knex',
};

const production: ConfigType = {
	database: 'knex',
};

export default {
	development,
	production,
};
